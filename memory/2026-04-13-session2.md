---
tags:
  - type/journal
  - domain/mastery
  - domain/ai
aliases:
  - Manifesto Craft Session
  - Whisper Setup Session
---

# 2026-04-13 — Session 2: Manifesto Craft + Whisper

## What Happened

### Manifesto Writing Craft
Trevor explained that manifestos emerge from collision with a domain, not from a planned curriculum. The depth tracks the need. Architecture emerged shallow because the need was specific, not existential. Technologist emerged deep because of lived failure in that domain.

Key insight: **The manifesto emerges at the intersection of a domain's invisible structure and your own lived collision with it.**

### Whisper Local Install Attempt
Tried to install `faster-whisper` with CUDA. Installs kept getting SIGKILL'd — OOM during CTranslate2 compilation. Need to retry with virtualenv or pre-built wheel.

Whisper (openai-whisper) is already installed and working with CUDA on the RTX 3080. The `faster-whisper` variant just wouldn't compile.

### Agent/Vault Access
Confirmed: any spawned agent inherits filesystem access and can read/write the vault. The vault at `k2m-sandbox/` is accessible to all agents on this machine.

## Source
Session: `agent:main:main` (webchat)
